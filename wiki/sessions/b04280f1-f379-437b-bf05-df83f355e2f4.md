---
tags: [go, cli, credit-card, bills, tui]
lastUsed: 2026-08-31
---

## What happened

Small follow-up session on the bills feature. User, from a screenshot: "it should show the name of the month related to that bill, like 'March'".

- Added a `MONTH` column to `pecunia cc bill`'s table, between `CARD` and `CLOSES`, derived from `ClosesOn`. A cycle closing 2026-09-10 now reads `September`:

```
CARD    MONTH      CLOSES      DUE         TOTAL   LEFT    STATUS
[AMEX2] September  2026-09-10  2026-09-30  $0.00   —       open
[AMEX2] August     2026-08-10  2026-08-31  $48.00  $48.00  closed
```

- Changed: `internal/bills/bill.go`, `internal/bills/ui.go` (table formatting), `internal/bills/ui_test.go` (new test pinning the column).
- A follow-up instruction — "add month to the picker too" — was handled by a subagent, which added the month name to the bill picker as well, confirmed by `go build ./...` and full `go test ./...` passing.
- Explicitly skipped, called out rather than assumed: year in the MONTH column (CLOSES already carries the year) and adding month to picker/details rows beyond what was asked — left for the user to request if wanted.
- Verification left as a next step: run `./dev cc bill AMEX2` to eyeball the real output.

Links: [[tasks/05-installments-and-credit-card-bill]] · [[decisions/0009-bills-as-rows-and-installments-as-transactions]]