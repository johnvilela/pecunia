---
tags: [go, cli, build]
lastUsed: 2026-08-31
---

## What happened

Added a `build.sh` script to compile the pecunia CLI into a single static, stripped binary, per the user's request: "Create a build script that will build this CLI into a single binary."

- Inspected repo state first: `go.mod`, `AGENTS.md`, `cmd/`/`internal/` layout, `cmd/main.go` header, `.gitignore`, `.githooks/*`.
- Wrote `build.sh` at the repo root (`/home/jv77/Documents/dev/pecunia/build.sh`).
- Verified: `chmod +x build.sh && ./build.sh` succeeds; `file pecunia` reports statically linked; `ls -lh pecunia` shows 8.0M; `./pecunia --help` runs and prints output.
- Deferred: version stamping, multi-platform build matrix, Makefile — noted to add when releasing.

Decision write-up: [[decisions/0003-static-stripped-build-script]]