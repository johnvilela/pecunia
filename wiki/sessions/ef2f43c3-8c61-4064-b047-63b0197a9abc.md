---
tags: [backup, s3, dropbox, git, oauth]
lastUsed: 2026-09-16
---

## What happened

Session opened on a broken `git pull`: "I tried running \"git pull\" but it throwed some error." Diagnosis and fix, then, in the same session, the `pecunia backup` feature ([[decisions/0026-backup-archives-to-a-directory-or-s3]]) was designed, built end to end, and opened as a pull request whose CI came back green. In a later continuation of the same session, that PR was approved and merged, and a second backup provider (Dropbox) was built as a follow-up.

### git pull stuck mid-rebase

Root cause: `pull.rebase=true`, and local `master` carried one commit (`387760e`, a wiki update) not yet on `origin/master`. An earlier `git pull` had started rebasing `387760e` onto `9078edc` (origin's squash-merge of PR #11, [[sessions/b410d49d-989f-41e2-bffb-553cf0bbf03e]]) and stopped on a conflict in `wiki/index.md` — both sides had appended a session line at the same spot (`01a06f2e` locally, `b410d49d` on origin). Every subsequent `git pull` then failed with "Pulling is not possible because you have unmerged files," which reads like a network error but is actually a stuck rebase.

Fixed by: keeping both index lines in the conflict; finding the resulting staged diff empty, because `387760e`'s content was already inside `origin/master`'s squash — so `git rebase --skip` was used instead of `--continue`, avoiding a duplicate commit. `master` came out equal to `origin/master` at `9078edc`.

The rebase reset also restored 16 `wiki/sessions/*.md` files that an earlier memoria pass had already moved to `wiki/trash/sessions/` (tagged `deleted`) — the reset replayed a tree state from before those deletions happened. Each restored file was confirmed identical to its trash copy apart from the `deleted` tag memoria adds (a two-line `diff`) before being deleted again, to put the working tree back to its intended post-trash state. Left for the user at that point: those 16 deletions plus an untracked `wiki/gotchas/escaped-quote-ends-quoted-value-early.md`.

### pecunia backup, built end to end

User: build a backup feature — database and notes to a directory, Dropbox, Google Drive or S3, with a configurable run frequency (times a day or week) — and asked to move the work to a new branch first ("Move to a new branch, get the changes there too").

Full design and implementation are recorded in [[decisions/0026-backup-archives-to-a-directory-or-s3]]: the `internal/backup` package, the `pecunia backup` command (`setup`, `run`, `list`, `restore`, `schedule`), `VACUUM INTO` snapshots, optional age encryption, a hand-rolled ~300-line S3 client with its own SigV4 signer (no AWS SDK), a `Local` directory provider, and a systemd user timer installed from an `N/day`/`N/week` grammar.

One research note worth keeping beyond the decision page: pinning the SigV4 signer to AWS's own published worked examples ("Examples: Signature Calculations in the Authorization Header") failed at first — `docs.aws.amazon.com` serves that page as a JS shell that redirects to a generic Welcome page when fetched with `curl`, with both a plain and a browser user-agent, and an `r.jina.ai` proxy fetch of the same URL also came back without the example. The two published signatures were instead cross-checked against five independent open-source SigV4 implementations found via `gh api search/code`, which is what caught one of the two test vectors having been mistyped by hand.

Verified live end to end against a scratch build: `setup` (local provider, with encryption), `run` twice, `list`, `restore -y` bringing a stale category count back down, and plain `pecunia backup` printing status with the passphrase never shown. Full suite, `gofmt`, `go vet` clean throughout. Version bumped 0.7.0 → 0.8.0. Landed as five commits: the wiki-trash/gotcha cleanup, the backup feature itself, the version bump, the README section, and the `decisions/0026` wiki page.

### Opened as PR #12, CI confirmed green

The branch used was `feat/backup` — confirmed by `git push -u origin feat/backup` in this session's own record, resolving the earlier uncertainty about whether the requested branch switch had actually happened. (The branch-creation command itself still isn't directly visible in the transcript, but the push and the five commits landing on it settle the question.)

After the push, the work was opened via `gh pr create` as **PR #12** — "feat(backup): back the database and notes up to a directory or S3" — with a body covering the `setup`/`run`/`list`/`restore`/`schedule` commands, the hand-rolled S3 SigV4 client, the restore safety guarantees, and the test coverage. A `Monitor` watch was set on the PR's CI; the check run reported `checks: pass`, confirming **PR #12's CI is green**. No merge was requested or attempted within this stretch of the session — PR #12 was left open at that point. (It was approved and merged later in the same session — see below.)

### Wiki consolidation interrupted by low memory

The session tried to flush its own wiki updates via the memoria consolidate job before finishing, but the background waits for it (`sleep 120`, then `sleep 180`) were both killed by the harness with "system is running low on memory" (a `ps` check at the time showed a `rspack-node` process at ~2.2 GB and a `java` process at ~1.5 GB). The consolidate job's own completion was never observed.

Because of that, the only wiki writes that actually landed on `feat/backup` within that part of the session were done by hand: `wiki/index.md` and `wiki/decisions/0026-backup-archives-to-a-directory-or-s3.md`, committed directly as `docs(wiki): decision 0026, backup archives to a directory or s3`. This session's own episodic page was not generated or committed before that stretch ended — the closing message flagged it explicitly as an open item.

## Continued: PR #12 merged, Dropbox added as a second backup provider

User: "PR 12 was approved, we can keep going." `gh pr view 12` was checked (state, review decision, mergeable, merge-state status) before continuing. A fresh `master` was pulled — picking up the now-merged PR #12 — and `feat/backup-dropbox` was branched off it, with the stashed uncommitted wiki edits (the status update to [[decisions/0026-backup-archives-to-a-directory-or-s3]] and this session's own page, including the "byte-identical (`cmp`)" wording above being corrected to "identical apart from the `deleted` tag") popped back onto the new branch and committed as `docs(wiki): record the backup session, mark 0026 merged`.

Dropbox landed as the second backup provider, per [[decisions/0026-backup-archives-to-a-directory-or-s3]]'s own plan (Dropbox and Google Drive each in their own PR, one file per provider behind the shared `Provider` interface). Full technical detail is on that decision page; in short: `internal/backup/dropbox.go` talks to Dropbox's HTTP API v2 directly (upload, download, paginated list, delete) with an OAuth 2 PKCE consent flow and no redirect URI, keeping only a refresh token in `backup.toml` and minting a short-lived access token per process. `internal/backup/config.go` and `provider.go` gained the `[dropbox]` config section and its `OpenProvider` case; `cmd/backup.go` gained the `askCode`/`dropboxConsent` flow and a Dropbox choice in the `setup` form. The private `size()` helper was exported as `backup.Size()` so `dropbox.go` could share it with `s3.go` and the command layer.

TDD throughout: the existing `providerSuite` from PR #12 ran unchanged against a new fake Dropbox server in `httptest`; `TestDropboxProvider` and `TestDropboxAuthorize` cover the provider and the consent flow directly; `cmd/backup_test.go` gained `TestBackupSetupDropbox`, driving the whole `setup --provider dropbox` path through two swappable seams — `backup.DropboxOAuth` (the token host) and `askCode` (the paste prompt) — so the OAuth exchange runs against `httptest` with no real Dropbox account. `gofmt`/`go vet`/`go test ./...` clean throughout. Not exercised against a real Dropbox app in this session — flagged explicitly in the wiki.

Version bumped 0.8.0 → 0.9.0. README and `wiki/index.md` were updated; the wiki gained a new "## Dropbox (PR 2, `feat/backup-dropbox`)" section on [[decisions/0026-backup-archives-to-a-directory-or-s3]], and its status line was updated a second time to "Dropbox followed on `feat/backup-dropbox` (v0.9.0)". Landed as four commits on `feat/backup-dropbox` — `feat(backup): dropbox provider with a pkce consent flow`, `chore: bump version to 0.9.0`, `docs: document the dropbox backup provider in the readme`, `docs(wiki): dropbox section in decision 0026` — pushed, and opened via `gh pr create` (title "feat(backup): dropbox provider with a PKCE consent flow", base `master` ← `feat/backup-dropbox`) as this session's final action. Whether that PR creation succeeded is not independently confirmed within this digest — the same pattern of an unconfirmed session-ending action this project has hit before ([[sessions/5321cd80-4dd0-4dea-85c3-391b008334d2]], [[sessions/52842417-3730-4f49-af46-f9654d86c3c8]], [[sessions/e22cf9b6-6521-4261-b791-0815460c124e]]).

Links: [[decisions/0026-backup-archives-to-a-directory-or-s3]] · [[decisions/0025-notes-as-markdown-files-with-a-computed-priority]] · [[decisions/0013-data-integrity-fixes-and-known-gaps]] · [[sessions/b410d49d-989f-41e2-bffb-553cf0bbf03e]] · [[sessions/e22cf9b6-6521-4261-b791-0815460c124e]] · [[rules/tdd]]